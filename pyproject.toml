[build-system]
requires = ["uv_build>=0.9.20,<0.10.0"]
build-backend = "uv_build"

[project]
name = "superjuggling"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
superjuggling = "superjuggling.__main__:main"

[dependency-groups]
dev = ["pre-commit", "pytest", "pyright[nodejs]", "ruff"]
